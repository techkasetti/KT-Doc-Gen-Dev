public with sharing class TemplateDeprecationService {
    public static Boolean deprecateTemplate(Id templateId, String note) {
        if (templateId == null) return false;
        // stub: set template flag
        return true;
    }
}
