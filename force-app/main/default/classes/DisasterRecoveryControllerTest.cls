@IsTest
private class DisasterRecoveryControllerTest {
    @IsTest static void testTriggerFailover() {
        Test.startTest();
        Boolean ok = DisasterRecoveryController.triggerFailover('c1');
        Test.stopTest();
        System.assertEquals(true, ok);
    }
}