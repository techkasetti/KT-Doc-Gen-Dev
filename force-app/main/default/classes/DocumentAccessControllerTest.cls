@IsTest
private class DocumentAccessControllerTest {
    @IsTest static void testGrantAccess_nulls() {
        Test.startTest();
        Boolean ok = DocumentAccessController.grantAccess(null, null, 'read');
        Test.stopTest();
        System.assertEquals(false, ok);
    }
}
